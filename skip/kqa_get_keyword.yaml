test_kqa_get_keyword:
  relevant_parameter: [Host, Access-Token, BotId, ID, TenantDomain]
  description: "根据id获取关键字信息"
  method: get
  url: ${Host}$/api/admin/keyword/KQA/get/${TenantDomain}$/${BotId}$/${ID}$
  headers:
    Access-Token: ${Access-Token}$
  assert:
    Status: 1
  check_db:
