test_validate_token:
  relevant_parameter: [Host, Access-Token]
  description: "验证token信息"
  method: post
  url: ${Host}$/api/auth/validateToken
  headers:
    Content-Type: application/json; charset=utf-8
    Access-Token: ${Access-Token}$
  assert:
    IsValid: True
